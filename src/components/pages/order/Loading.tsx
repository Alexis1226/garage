import { FlexCenterSection } from '../../../styles/common/FlexCenterSection';

const Loading = () => {
  return (
    <FlexCenterSection>
      목록을 <br />
      불러오고 있습니다.
    </FlexCenterSection>
  );
};

export default Loading;
